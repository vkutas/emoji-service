name: Publish release on Docker Hub

on:
  push:
    branches:
      - 'main'

jobs:
  run_tests:
    runs-on: [ubuntu-latest]
    steps:
      - uses: actions/checkout@master

      - uses: actions/setup-python@v2
        with:
          python-version: '3.8'
          architecture: 'x64'

      - name: Install requirements
        run: pip install -r app/requirements.txt
             pip install pylint

      - name: Run pylint on app sources
        run: pylint --fail-under=8 app/src/* 

      - name: Run pylint on test sources
        run: pylint --fail-under=3 app/tests/* 

      - name: Run tests
        run: coverage run app/tests/tests.py

      - name: Tests report
        run: coverage report
  
  build_and_publish:
    needs: [run_tests]
    runs-on: [ubuntu-latest]
    env:
      LOGIN: ${{ secrets.DOCKER_HUB_LOGIN }}
      NAME: ${{ secrets.DOCKER_HUB_REPO }}
    steps:
      - uses: actions/checkout@master

      - name: Build image
        run: docker build -t $LOGIN/$NAME:dev -f Dockerfile .

      - name: Login and Push
        run:  echo ${{ secrets.DOCKER_HUB_PASS }} | docker login -u ${{ secrets.DOCKER_HUB_LOGIN }} --password-stdin && docker push $LOGIN/$NAME:dev

             

            
